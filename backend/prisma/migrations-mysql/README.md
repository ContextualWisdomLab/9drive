# MySQL migrations

The committed MySQL migration history lives in `../migrations/` (the default Prisma migrations directory used by `schema.mysql.prisma`).
Use `npm run prisma:migrate:mysql` / `npm run db:migrate:deploy:mysql` to manage them.
